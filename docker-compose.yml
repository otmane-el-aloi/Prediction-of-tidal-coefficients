version: "3.7"

services:

  flask:
    build: ./flask
    container_name: PALL
    restart: always
    environment:
      - APP_NAME=tidal-predicition
    expose:
      - 8080

  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      - "80:80"